#include <iostream>
#define length 10
using namespace std;
int nextEmptyPosition=0;
int arr[length];
void _insert(int value)
{
    int nextEmptyPosition=0;
    int i;
    for(i=0;i<10;i++)
        cout<<arr[length]<<endl;
    cin>>arr[length];
    if(int nextEmptyPosition=10)
        cout<<"Array is total full"<<endl;

}
